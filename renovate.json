{
  "extends": ["group:all", ":dependencyDashboard"],
  "enabledManagers": ["regex"],
  "regexManagers": [
    {
      "fileMatch": ["^src/pytest_servers/gcs.py$"],
      "matchStrings": [
        "fake_gcs_image = \"(?<depName>.*?)\"",
        "fake_gcs_tag = \"(?<currentValue>.*?)\""
      ],
      "datasourceTemplate": "docker"
    },
    {
      "fileMatch": ["^src/pytest_servers/azure.py$"],
      "matchStrings": [
        "azurite_image = \"(?<depName>.*?)\"",
        "azurite_tag = \"(?<currentValue>.*?)\""
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
